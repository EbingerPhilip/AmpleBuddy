Im richtigen *.html file

<header>
// Code
<script src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/4.8.1/socket.io.min.js" integrity="sha512-8ExARjWWkIllMlNzVg7JKq9RKWPlJABQUNq6YvAjE/HobctjH/NA+bSiDMDvouBVjp4Wwnf1VP1OEv7Zgjtuxw==" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
// Html script laden
// rest vom Code



In dem *.ts file welches bei dem Aufrufen der Chats ausgeführt wird:
// Ganz oben:
const socket = io('ws://localhost:3500');

// Sends data
// Called when entering chat
socket.emit('enterChat', {
            userId: <value>,
            chatId: <value>
        });

// Called when sending a Message
socket.emit('sendmessage', {
            sender: <value>,
            chatId: <value>,
	    text: <value>
        });

// Called when ededing a message
socket.emit('editmessage', {
            sender: <value>,
            text: <value>,
	    messageId: <value>
        });

// Called when deleating message
socket.emit('deletemessage', {
            sender: <value>,
	    messageId: <value>
        });

// Called when getting all messages
socket.emit('getallmessages', {
            userId: <value>,
            chatId: <value>
        });


// Recives data
// Recives the message
socket.on('sendmessage', (data) => {
	const { sender, chatId, text } = data;
	// Data from the send message
});

// Server Responses
socket.on('serverResponseError', (data) => {
	const { status, error } = data;
	// Data from the server error
});

// Server allMessages
socket.on('allMessages', (data) => {
	const { status, success, data} = data;
	// Data from all messages
});

// Server deleteMessages
socket.on('deleteMessages', (data) => {
	const { status, success, message }= data;
	// Data from delete messages
});

// Server editMessage
socket.on('editMessage', (data) => {
	const { status, success, message }= data;
	// Data from editMessage
});

// Server sendMessageResponse
socket.on('sendMessageResponse', (data) => {
	const { status, success, messageId}= data;
	// Data from sendMessageResponse
});

